# CRUD-Web-App-Laravel
Web app created with PHP Laravel that supports CRUD operations (Create, Read, Update, Delete) to a clinic database. To start it go to adresa_acces.txt and follow the instructions.
